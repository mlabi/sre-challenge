apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: registry
spec:
  selector:
    app.kubernetes.io/name: registry
  ports:
    - name: https
      port: 443
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
